import { View, Text } from 'react-native';
import React, { memo } from 'react';

const BinScreen = () => {
  return (
    <View>
      <Text>BinScreen</Text>
    </View>
  );
};

export default memo(BinScreen);
